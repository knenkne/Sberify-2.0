!(function () {
    var e = 'dark-mode',
        t = 'light-mode';
    var o = null;
    try {
        o = localStorage.getItem('darkMode');
    } catch (e) {
        console.error(e);
    }
    var a,
        l = null !== o;
    if ((l && (o = JSON.parse(o)), l))
        (a = o),
            document.documentElement.classList.add(a ? e : t),
            document.documentElement.classList.remove(a ? t : e);
    else {
        var n = document.documentElement.classList.contains(e);
        localStorage.setItem('darkMode', JSON.stringify(n));
    }
})();
